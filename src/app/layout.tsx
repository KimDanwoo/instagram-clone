import AppLogo from "@/components/icons/AppLogo";
import "./globals.css";
import { Open_Sans } from "next/font/google";
import AppLogoIcon from "@/components/icons/AppLogoIcon";
import LookIcon from "@/components/icons/LookIcon";
import AddPostIcon from "@/components/icons/AddPostIcon";

const openSans = Open_Sans({ subsets: ["latin"] });

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={openSans.className}>
      <body className="flex flex-col md:flex-row ">
        <header className="flex justify-between md:flex-col h-16 md:h-screen md:w-16 md:border-r lg:w-80 border-b md:p-3 md:justify-start lg:pt-10">
          <h1 className="flex items-center px-5 md:hidden lg:block">
            <AppLogo />
          </h1>
          <h1 className="hidden md:flex justify-center items-center lg:hidden mb-11 p-2 rounded-xl hover:bg-gray-100">
            <AppLogoIcon />
          </h1>
          <ul className="flex md:flex-col items-center w-40 justify-around md:w-full lg:items-start lg:p-2 lg:pt-11">
            <li className="p-2 rounded-xl hover:bg-gray-100 lg:w-full lg:flex">
              <LookIcon />
              <span className="hidden lg:inline-block ml-5">검색</span>
            </li>
            <li className="p-2 rounded-xl hover:bg-gray-100 lg:w-full lg:flex ">
              <AddPostIcon />
              <span className="hidden lg:inline-block ml-5">만들기</span>
            </li>
          </ul>
        </header>
        <div>{children}</div>
      </body>
    </html>
  );
}
